transformers>=5.7.0
# torch + torchvision + torchaudio are NOT listed here. They're installed
# separately from the matching PyTorch index (CUDA build on GPU, CPU wheel
# otherwise) by python-setup.js. Listing them here would let the default
# PyPI wheel override the CUDA build and break torchaudio's DLL linkage
# (WinError 127 on Windows when audio feature extractors try to load).
pillow
soundfile
librosa
numpy
accelerate
huggingface_hub
diffusers
timm
sentencepiece
protobuf
scipy
